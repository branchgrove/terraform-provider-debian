---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "debian_command Action - debian"
subcategory: ""
description: |-
  debian_command runs an arbitrary command on the remote host and reports stdout, stderr, and exit code via progress messages.
---

# debian_command (Action)

`debian_command` runs an arbitrary command on the remote host and reports stdout, stderr, and exit code via progress messages.



<!-- action schema generated by tfplugindocs -->
## Schema

### Required

- `command` (String) Command to execute.
- `ssh` (Attributes) SSH connection configuration. Authentication can be specified here or inherited from the provider's `private_key` / `private_keys`. (see [below for nested schema](#nestedatt--ssh))

### Optional

- `allow_error` (Boolean) If `true`, non-zero exit codes are not treated as errors. Defaults to `false`.
- `env` (Map of String) Environment variables to set for the command.
- `stdin` (String) Standard input to pass to the command.

<a id="nestedatt--ssh"></a>
### Nested Schema for `ssh`

Required:

- `hostname` (String) The remote host to connect to.

Optional:

- `host_key` (String) Public key of the remote host in authorized_keys format. If set, the server's identity is verified against this key. If not set, all host keys are accepted (insecure).
- `password` (String) Password to authenticate the user on the remote host. Conflicts with `private_key` and `public_key`.
- `port` (Number) Port that the remote host uses for ssh.
- `private_key` (String) Private key to authenticate with the remote host. Conflicts with `password` and `public_key`. When omitted, the provider's `private_key` is used.
- `public_key` (String) Public key whose corresponding private key is looked up in the provider's `private_keys` map. Conflicts with `private_key` and `password`.
- `user` (String) User on the remote host, the default is `root`.
